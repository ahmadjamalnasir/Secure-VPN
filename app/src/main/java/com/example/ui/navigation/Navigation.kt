package com.example.ui.navigation

object ScreenAuth {
    const val route = "auth"
}

object ScreenConnect {
    const val route = "connect"
}

object ScreenServers {
    const val route = "servers"
}

object ScreenSubscription {
    const val route = "subscription"
}
